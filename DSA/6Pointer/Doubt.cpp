#include <iostream>
using namespace std;

int main()
{

    // int arr[5] = {1, 3, 5, 7, 9};
    // int *ptr = arr;
    // cout << *ptr << endl;
    // cout << *(ptr + 1) << endl;
    // cout << *ptr + 1 << endl;

    // // ERROR
    // void *ptr1 = (void *)(arr);
    // cout << *ptr1 << endl;
    // cout << *(ptr1 + 1) << endl;

    // char *cptr = "OmJindal";
    // cout << cptr << endl;
    // cout << *cptr << endl;

    // // IT WILL NOT COMPILE AND RUN
    // cptr[5] = 'd';
    // cout << cptr << endl;
    // cout << *cptr << endl;

    // int a[5] = {1, 3, 5, 7, 9};
    // int *ptr = a;
    // cout << ptr << endl;
    // cout << *ptr << endl;
    // cout << *ptr + 1 << endl;
    // cout << *(ptr) + 1 << endl;
    // cout << *(ptr + 1) << endl;
    // cout << ptr[2] << endl;
    // // cout << *ptr[2] << endl;// ERROR

    // cout << endl;

    // int (*ptr1)[5] = &a;
    // cout << int(ptr1) << endl;
    // cout << int(*ptr1) << endl;
    // cout << int(*ptr1 + 1) << endl;
    // cout << int(*(ptr1 + 1)) << endl;
    // cout << (*ptr1)[3] << endl; // IT OPEN UP AS **(ptr + 3)

    // cout << endl;

    // int *ptr2 = &a[3];
    // cout << ptr2 << endl;
    // cout << *ptr2 << endl;
    // cout << *ptr2 + 1 << endl;
    // cout << *(ptr2 + 1) << endl;

    // ------------------------------------------------------------------------------------------

    // int a = 5;
    // int b = 6;
    // int c = 7;

    // int *ptr[3];
    // *ptr = &a;
    // // ptr[0] = &a;
    // **ptr = 20;
    // // ptr[1] = &b;
    // *(ptr + 1) = &b;
    // **(ptr + 1) = 30;
    // ptr[2] = &c;

    // cout << *ptr[0] << endl;
    // cout << *ptr[1] << endl;
    // cout << *ptr[2] << endl;

    // ------------------------------------------------------------------------------------------

    // int x = 10;
    // int *ptr = &x;
    // cout << *ptr + 10 * *ptr - 3 * *ptr << endl;

    // int x = 5, y = 6, z = 7;
    // int *arr[5] = {&x, &y, &z};
    // cout << *arr[1] << endl;
}